function func() {
    // Original Array
    let arr = ['Portal', 'Science',
        'Computer', 'GeeksforGeeks'];
    console.log(arr);

    // Reversed array
    let new_arr = arr.reverse();
    console.log(new_arr);
}
func();
